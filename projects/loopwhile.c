#include <stdio.h>

int	main()
{
	int x;
	//A variable Do can be use to create a invert while. Looks like speaking in English.
	x = 1;
	while(x <= 10)
	{
		printf("%d\n", x);
		x++;
	}
// There is always a way to create a endless look by setting the value 1 after the while(1) will never stop.
	return (0);
}
